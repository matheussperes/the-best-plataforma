// stub — implementação vem na Fase 2

import type { QuizOption } from '../../data/quizSteps';

interface OptionCardProps {
  opt: QuizOption;
  selected: boolean;
  onClick: () => void;
}

export function OptionCard(_props: OptionCardProps) {
  return null;
}
