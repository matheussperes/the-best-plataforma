// stub — implementação vem na Fase 2
import React from 'react';

interface DisplayProps {
  children?: React.ReactNode;
  size?: number;
  color?: string;
  style?: React.CSSProperties;
}

export function Display(_props: DisplayProps) {
  return null;
}
